# .env.example
# Copy this to .env and fill in your values.
# These values go into js/supabase.js — there is no server-side
# processing so the anon key is safe to expose in the browser.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
